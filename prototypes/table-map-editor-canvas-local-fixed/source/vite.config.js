import { defineConfig } from "vite";
import react from "@vitejs/plugin-react";

export default defineConfig({
  base: "/arena-test/prototypes/table-map-editor-canvas-local-fixed/",
  plugins: [react()],
});
